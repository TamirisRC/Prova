<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Tela de Edição</title>
</head>

<body>
    <div class="container">
        <h1>Tela de Edição</h1>
        <form>
            {{#each post}}
            {{#dataValues}}
            <form action="/atualizar" method="POST">
                <input type="hidden" name="id" class="form-control" id="nome" value="{{id}}">
                <div class="mb-3 text-start">
                    <label class="form-label">Nome:</label>
                    <input type="name" name="nome" class="form-control" id="nome" value="{{nome}}">
                </div>
                <div class="mb-3 text-start">
                    <label class="form-label">Endereço:</label>
                    <input type="text" name="endereco" class="form-control" id="endereco" value="{{endereco}}">
                </div>
                <div class="mb-3 text-start">
                    <label class="form-label">Bairro:</label>
                    <input type="text" name="bairro" class="form-control" id="bairro" value="{{bairro}}">
                </div>
                <div class="mb-3 text-start">
                    <label class="form-label">CEP:</label>
                    <input type="text" name="cep" class="form-control" id="cep"
                        value="{{cep}}">
                </div>
                <div class="mb-3 text-start">
                    <label class="form-label">Cidade:</label>
                    <textarea name="cidade" class="form-control" placeholder="" id="cidade"
                        style="height: 100px">{{cidade}}</textarea>
                </div>
                <div class="mb-3 text-start">
                    <label class="form-label">Estado:</label>
                    <textarea name="estado" class="form-control" placeholder="" id="estado"
                        style="height: 100px">{{estado}}</textarea>
                </div>
                <div class="mb-3 text-start">
                    <button type="submit" class="btn btn-primary">Atualizar</button>
                </div>
            </form>
            {{/dataValues}}
            {{/each}}
    </div>
    <div class="col-1">
        &nbsp;
    </div>
    </div>
    </div>